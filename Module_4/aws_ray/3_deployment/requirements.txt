# ============================================================================
# RAY PIPELINE DEPENDENCIES
# ============================================================================
# Python 3.9+

# Core Ray
ray[default]==2.9.0
ray[data]==2.9.0

# AWS Integration
boto3==1.34.34
botocore==1.34.34

# Document Processing
docling==1.8.0
python-docx==1.1.0
python-pptx==0.6.23
openpyxl==3.1.2

# AI/ML Libraries
# NOTE: sentence-transformers, transformers, torch removed —
# embeddings are now generated exclusively via the OpenAI API.
# AWS Comprehend removed — enrichment is now handled by OpenAI gpt-4o-mini.
openai==1.10.0

# Vector Database
pinecone-client==3.0.0

# PDF Processing
pdf2image==1.16.3
Pillow==10.2.0

# Data Processing
pandas==2.2.0
numpy==1.26.3

# Retry & Progress
# tenacity: exponential-backoff retry used by openai_embeddings.py
# tqdm:     batch progress bars in generate_embeddings()
tenacity==8.2.3
tqdm==4.66.1

# Utilities
python-dotenv==1.0.0
tabulate==0.9.0

# Logging and Monitoring
structlog==24.1.0
